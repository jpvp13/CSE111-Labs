SELECT c_address, c_phone, c_acctbal 
FROM customer 
WHERE c_custkey = '127';
/* 
SELECT chooses which attributes
FROM chooses from which table to pull attributes from
WHERE allows us to specific more specifically which attribute to focus on
*/


